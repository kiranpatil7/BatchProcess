spring.application.name=SpringBatch
logging.level.org.springframework.batch=DEBUG
logging.level.org.springframework.batch.core=DEBUG
logging.level.org.springframework.batch.core.step=DEBUG

#database configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/mydb
spring.datasource.username=root
spring.datasource.password=root
#Spring Boot 2.0 includes HikariDataSource by default
spring.datasource.type = com.zaxxer.hikari.HikariDataSource

spring.batch.jdbc.initialize-schema=always

#if you want to add another database sceama here u can add also this way add auto wired in configuration file
#if you are havaing one datasource then url is ok if not then use jdbc-url
#We have to create beans for datasource if have multiple database
#pring.sysdatasource.url=jdbc:mysql://localhost:3306/sys
#spring.sysdatasource.username=root
#spring.sysdatasource.password=root




